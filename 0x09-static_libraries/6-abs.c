#include "main.h"

/**
* _abs - Calculates absolute value of integer.
* @n: Integer for which to calculate absolute value.
*
* Return: Absolute value of n.
*/

int _abs(int n)
{
	if (n < 0)

		return (-n);

	else

		return (n);
}
